name: named_routes_with_params_and_di
description: A new Flutter project.
version: 1.0.0+1
environment:
  sdk: ">=2.1.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  get_it: ^1.0.3+2
  shared_preferences: ^0.5.3+1
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true